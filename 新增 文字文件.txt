<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>苗栗旅館綠指標地圖系統</title>
  <style>
    /* 全域字體與背景 */
    body {
      margin: 0;
      font-family: "Noto Sans TC", sans-serif;
      background: url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
    }

    /* 玻璃擬態卡片 */
    .glass {
      background: rgba(255, 255, 255, 0.15);
      border-radius: 15px;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      padding: 20px;
      margin: 10px;
    }

    header {
      text-align: center;
      padding: 30px;
      font-size: 2em;
      font-weight: bold;
      letter-spacing: 2px;
    }

    .container {
      display: flex;
      flex-direction: row;
      height: calc(100vh - 120px);
    }

    .sidebar {
      width: 300px;
      display: flex;
      flex-direction: column;
    }

    .map {
      flex: 1;
      margin: 10px;
      border-radius: 15px;
      overflow: hidden;
    }

    select, input {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      border-radius: 10px;
      border: none;
      outline: none;
      background: rgba(255,255,255,0.2);
      color: #fff;
    }

    select option {
      color: #000;
    }

    #map {
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.3);
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      font-size: 1.2em;
    }
  </style>
</head>
<body>
  <!-- 標題區 -->
  <header class="glass">
    苗栗旅館旅宿綠指標地圖系統
  </header>

  <div class="container">
    <!-- 側邊欄：API設定區 + 地點選單 -->
    <div class="sidebar">
      <div class="glass">
        <h3>API 設定區</h3>
        <input type="text" placeholder="輸入 API Key">
        <input type="text" placeholder="輸入資料來源 URL">
      </div>
      <div class="glass">
        <h3>地點選單</h3>
        <select id="locationSelect">
          <option value="miaoli">苗栗市</option>
          <option value="sanyi">三義</option>
          <option value="nanzhuang">南庄</option>
          <option value="taian">泰安</option>
          <option value="gongguan">公館</option>
        </select>
      </div>
    </div>

    <!-- 地圖展示區 -->
    <div class="map glass">
      <div id="map">地圖展示區（可嵌入 Mapbox / Google Maps）</div>
    </div>
  </div>

  <script>
    // 簡單互動：選擇地點時更新地圖文字
    const locationSelect = document.getElementById('locationSelect');
    const mapDiv = document.getElementById('map');

    locationSelect.addEventListener('change', () => {
      const loc = locationSelect.options[locationSelect.selectedIndex].text;
      mapDiv.innerHTML = `目前顯示：${loc} 的旅館綠指標`;
    });
  </script>
</body>
</html>
